<%- model_class = Tipdoc -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id) %></th>
      <th><%= model_class.human_attribute_name(:iddoc) %></th>
      <th><%= model_class.human_attribute_name(:docname) %></th>
      <th><%= model_class.human_attribute_name(:docser) %></th>
      <th><%= model_class.human_attribute_name(:docnum) %></th>
      <th><%= model_class.human_attribute_name(:datebeg) %></th>
      <th><%= model_class.human_attribute_name(:dateend) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @tipdocs.each do |tipdoc| %>
      <tr>
        <td><%= link_to tipdoc.id, tipdoc_path(tipdoc) %></td>
        <td><%= tipdoc.iddoc %></td>
        <td><%= tipdoc.docname %></td>
        <td><%= tipdoc.docser %></td>
        <td><%= tipdoc.docnum %></td>
        <td><%= tipdoc.datebeg %></td>
        <td><%= tipdoc.dateend %></td>
        <td><%=l tipdoc.created_at %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_tipdoc_path(tipdoc), :class => 'btn btn-mini' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      tipdoc_path(tipdoc),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_tipdoc_path,
            :class => 'btn btn-primary' %>
